# Generated by rpi-streamer. Do not edit this file directly.
server {
    listen __LISTEN__;
    server_name _;
    root "__SITE_ROOT__";
    index index.html;
    autoindex off;
    server_tokens off;
    sendfile on;
    etag on;

    location = /healthz {
        access_log off;
        default_type application/json;
        add_header Cache-Control "no-store" always;
        return 200 '{"status":"ok"}\n';
    }
    location ~ (^|/)\. { return 404; }
    location /assets/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }
    location = / {
        try_files /index.html =404;
        add_header Cache-Control "no-cache" always;
    }
    location ~ \.html$ {
        try_files $uri =404;
        add_header Cache-Control "no-cache" always;
    }
    location ~* ^/media/(?<media_file>.+\.mp4)$ {
        alias "__MEDIA_ROOT__$media_file";
        disable_symlinks on from=__MEDIA_ROOT__;
        default_type video/mp4;
        limit_except GET HEAD { deny all; }
        add_header Cache-Control "private, max-age=0, must-revalidate" always;
    }
    location /media/ { return 404; }
    location / { try_files $uri $uri/ =404; }
}
